#pragma once

#include <string>
#include <vector>

///// Get standard headers from compiler
// std::vector<std::string> detectStandardHeaders(std::string compiler = "g++");

////  Does not work, use global clangd configuration instead
// void setStandardHeaders(std::string sourceCompiler = "g++");
